#1. Explain the difference between map(), filter(), and reduce() in Python.
# What does each function return?
# When should you prefer lambda functions over normal functions?
print("map(): Applies a function to every element. Returns map object (convert using list())")
print("filter(): Keeps only elements satisfying a condition. Returns filter object")
print("reduce(): Combines all elements into one value. Returns a Single value")

#When to use Lambda Functions
print("Use Lambda when: Function is short. Used only once. Makes code shorter.")

#When to use normal Functions
print("Use Normal when: Logic is long. Function will be reused.")